# OS
Thumbs.db
.DS_Store
Desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# nvim - don't commit transient state, keep lock for reproducibility if you want
# Uncomment to ignore lock:
# nvim/lazy-lock.json
nvim/.cache/
nvim/.undo/

# wezterm - no cache, saved wallpaper is per-machine state
wezterm/saved-backgrounds.lua
# powershell - no secrets (repo is public)
